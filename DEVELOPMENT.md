# 개발 노트

코드를 만지기 전에 알아야 할 것만 담는다. 무엇을 하는 프로그램인지는
[README.md](README.md) 에 있다.

## 구조

```
electron/
  main.js       창·트레이·IPC. 파일 작업은 notes.js 에 위임
  notes.js      노트 읽기/쓰기, 이동, 이미지, 이름 검증   (Electron 비의존)
  document.js   body splice, 손실 판정, 문서 만들기        (Electron 비의존)
  encoding.js   UTF-8 인지 확인하고 아니면 거절            (Electron 비의존)
  search.js     본문 검색과 mtime 캐시                     (Electron 비의존)
  config.js     설정 JSON, 서식 프리셋                      (Electron 비의존)
  seed.js       양식 폴더에 빠진 파일 채우기                (Electron 비의존)
  update.js     새 판이 있는지만 확인                       (Electron 비의존)
  preload.js    contextBridge API
src/
  main.js       렌더러 (트리·iframe 편집기·설정 창·검색)
  saving.mjs    파일이 받은 것과 받지 않은 것의 판단
  dark-note.mjs 노트 색을 어두운 대역으로 옮기는 계산
  datetime.mjs  날짜·시각 형식
  styles.css    앱 UI 색과 배치
build/
  installer.nsh 제거할 때 노트를 지킬지 묻는 NSIS 매크로
test/           검증 스위트 + fixtures/ + support/
```

두 가지 원칙:

- **파일에 쓰는 모든 경로는 `electron/notes.js` 를 지난다.** 테스트가 앱과 같은 코드를
  돌리는 이유다.
- **Electron 에 의존하지 않는 모듈로 뺄 수 있으면 뺀다.** node 로 바로 테스트되고,
  나중에 런타임을 갈아도 그대로 옮겨간다.

## 저장이 어떻게 동작하는가 — 가장 중요한 부분

노트는 임의의 HTML 이다. 표 레이아웃, `<font>`, 인라인 스타일, 문서 자체의 `<head>`
스타일시트. 그래서 **저장은 문서를 다시 만들지 않는다.** 디스크의 파일을 읽어
`<body>` 안쪽만 갈아끼우고 나머지 바이트는 그대로 둔다.

이 프로젝트는 그것이 지켜지지 않아 실제 노트가 손상된 적이 있다. 편집기가 스키마에
없는 것을 버리고, 저장이 그 결과로 문서를 재구축했다. 표와 641개의 style 속성이
사라졌다.

지금 지키는 것들:

- **더티일 때만 쓴다.** 열기만 한 파일은 디스크에 닿지 않는다
- **`inspectWrite` 가 구조 전멸을 잡는다.** 표·글꼴·style 속성이 통째로 사라지는
  저장은 거부한다. **이 가드를 끄지 말 것**
- **`<body>` 가 없어지는 저장도 거부한다.** 그러면 splice 할 곳이 없어져 다음 저장이
  문서를 재구축한다
- **`_backup/` 에 직전 판 한 세대.** 트리 구조를 미러링하고 노트를 따라 움직인다
- **원자적 쓰기** — 임시 파일에 쓴 뒤 rename. 전원이 나가도 반쯤 쓰인 노트는 없다
- **외부 변경은 mtime 으로 감지**해 충돌로 알린다

가드가 막으면 그 수정은 편집기 안에만 남는다. 그 상태를 `src/saving.mjs` 가 다룬다 —
상태 줄은 `저장 보류` 를 유지하고(쓸 것이 없다는 것과 다 저장됐다는 것은 다르다),
그것을 누르면 거부된 이유를 보여주며 그래도 쓸지 묻는다. **가드에 문을 내되 소리
없이 지나가게 두지는 않는다**가 여기 원칙이다.

## 편집기

노트를 `<iframe>` 에 **실제 문서 그대로** 띄우고 `designMode` 로 그 자리에서 편집한다.
스키마 파싱이 없으므로 표·`<font>`·인라인 스타일이 편집을 거쳐도 살아남는다.

**주입하는 것은 `<head>` 에만, 그리고 `</head>` 바로 앞에.** 밤 모드의 노트 색은 생성한
CSS 를 노트 문서에 넣어 만드는데, 저장이 `<body>` 만 가져가기 때문에 파일에 닿지 않는다.
`<body>` 안에 무언가를 넣으면 그 순간 파일에 섞인다.

**위치도 중요하다.** `<head>` 맨 앞에 넣으면 노트 자체 스타일이 뒤에 와서 명시도가 같을
때 이긴다 — `td { color: #222 }` 하나에 밤 모드가 무력해진다. 이 앱이 만드는 노트가 전부
그랬다. 뒤에 넣으면 요소 선택자는 우리가 이기고, `class` 로 지정한 색은 명시도가 높아
그대로 남는다. `<base>` 만은 맨 앞에 둔다 — `<head>` 안의 URL 보다 먼저 와야 한다.

`execCommand` 는 **커서가 놓인 블록에 높이가 있을 때** 믿을 수 있다. 높이 0인 블록에서는
성공했다고 보고하면서 텍스트를 엉뚱한 곳 — 보통 `<body>` 직속 — 에 떨군다. 자식 노드가
없어서가 아니라 높이 때문이다. 재서 확인했다.

그래서 편집 모드에서만 한 줄을 주입한다.

```css
p:empty, div:empty, td:empty, li:empty { min-height: 1.2em; }
```

빈 블록은 높이가 0이라 클릭할 면적이 없다. 손으로 쓴 HTML 에는 드물지 않은 자리다.
`<br>` 로 채우면 고쳐지지만 그만큼 문서가 길어진다 — **편집기에 들어갔다는 이유로
문서를 바꾸지 않는다.** 이 CSS 는 `<head>` 로 가므로 파일에 닿지 않고,
편집을 나가면 제거된다.

날짜 삽입(`F5`)은 `execCommand` 를 쓴다. 그래야 되돌리기 스택에 들어간다. 직접 DOM 에
넣으면 `Ctrl-Z` 가 **그 앞의 타이핑을 지우고 도장은 남긴다.** 높이가 없는 블록이거나
명령이 거절당하면(포커스 없는 창이 그렇다) 직접 삽입으로 물러나고, 그 한 번만 되돌릴 수
없다.

**편집기가 만든 소스는 한 줄로 뭉친다.** Chromium 이 형제 요소 사이에 아무것도 넣지 않고
직렬화하기 때문이다. `electron/document.js` 의 `breakBlocks()` 가 저장 직전 블록 사이만
줄을 나누고 앞 블록의 들여쓰기를 물려받는다. 손으로 쓴 HTML 은 원래 블록 경계에서 줄이
나뉘어 있다 — 새로 만든 규칙이 아니라 되찾은 것이다. `<pre>` 안은 건드리지 않는다.

소스 탭 미리보기도 **같은 함수를 쓴다.** preload 가 `sandbox: false` 라 `electron/document`
를 그대로 공유한다. 복사본을 만들면 미리보기와 파일이 어긋난다.

## 표

표를 만드는 기능은 나중에 붙었다. 그전까지 노트의 표는 붙여넣거나 소스 탭에서 직접 쓴
것이었다. 지금은 도구모음의 격자로 만들고, 가장자리에서 행·열을 넣고 빼고, 열 경계선을
끌어 너비를 바꾼다.

- **행·열 편집은 설정을 켜야 나온다** (`editor.tableEditing`, 기본 꺼짐). 노트 양식은
  본문이 바깥 표 안에 있어서, 켜두면 글 쓰는 내내 삭제 마커가 따라다닌다
- **손잡이는 앱 쪽에 있고 iframe 위에 좌표를 맞춰 띄운다.** 노트 안에 넣으면 파일에
  섞인다. 그래서 드래그는 **포인터 캡처**로 잡는다 — 마우스가 iframe 으로 들어가는
  순간 부모 창은 이동도 놓는 것도 듣지 못하고, 그러면 드래그가 끝나지 않아 나중에
  마우스만 움직여도 폭이 튄다
- **표를 바꿀 때는 통째로 교체한다.** 행 추가에 해당하는 `execCommand` 가 없으므로
  직접 DOM 을 고치면 `Ctrl-Z` 가 엉뚱한 것을 지운다. 요소 교체는 브라우저가 기록한다
- **병합된 셀이 있으면 거절한다.** `colspan` 이 끼면 셀의 인덱스가 열 번호와 달라 표가
  어긋난다
- **너비는 테두리 상자로 재고 내용 상자로 쓴다.** 잰 값을 그대로 넣으면 셀마다 여백이
  되붙어 표가 계속 넓어진다. 열을 넣고 뺄 때는 비율을 지켜 다시 나눈다 — 안 그러면
  새 열이 받을 공간이 없어 보이지 않는다
- **위아래 화살표는 열을 따라간다.** Chromium 은 줄 단위로 움직이는데 한 줄짜리 셀에는
  다음 줄이 없어서 오른쪽 칸으로 가버린다

## 색

두 곳으로 나뉜다.

- **앱 UI** — `src/styles.css` 의 `:root` (어두운 테마) 와 `[data-theme=light]`
- **노트 내용** — `src/dark-note.mjs` 의 `darkenBackground` / `lightenForeground`

노트 색은 반전이 아니라 **색상(hue)을 지키고 밝기만** 옮긴다. 배경은 어두운 대역으로,
글자는 밝은 쪽으로. `#3c62c6` → `#1f2a47`. 반전하면 파랑이 밝아지고 그 위 흰 글씨가
검게 되어 관계가 무너진다.

## 테스트

결함을 고칠 때는 **재현 테스트를 먼저 만들고, 수정을 빼서 그 테스트가 실패하는지
확인한다.** 그러지 않으면 무엇을 고쳤는지 확신할 수 없다.

이 프로젝트에서 실제로 있었던 일:

- **드래그&드롭이 동작하지 않았다.** 리스너는 붙어 있고 하이라이트도 떴다. `node
  --check` 도 모듈 테스트도 통과했다. → `test/verify-tree-dnd.js` 가 실제 렌더러를
  구동하는 이유
- **없는 함수를 부르는 코드가 문법 검사를 통과했다.** 노트를 삭제해야 드러났다.
  → `test/verify-ipc.js` 가 모든 채널을 실제로 호출하는 이유
- **제거할 때 뜨는 질문이 뜨지 않았다.** 자동 검사는 전부 통과하고 있었다. 사람이
  누르는 경로를 아무도 밟지 않았기 때문이다. → `test/verify-installer.js` 가 실제
  대화상자를 `test/support/dialog.ps1` 로 눌러 보는 이유
- **소스 탭에 친 내용이 파일에 저장되지 않았다.** 상태 표시줄에는 "저장됨" 이 떴다.
  `writeNote` 는 멀쩡했고, 렌더러가 거기까지 가지 못했다. 아무도 소스 탭에 타이핑해
  보지 않았기 때문이다. → `verify-tree-dnd.js` 가 실제로 쳐 보고, 다른 노트로 갔다가
  돌아와서 남아 있는지 확인하는 이유
- **낡은 툴팁이 여러 차례 살아남았다.** 아무도 문구를 읽지 않았다.
  → `test/verify-wording.js`

렌더러 테스트는 오프스크린 창에서 돌아 포커스를 받지 못한다. 포커스가 필요한 동작은
그 환경에서 검증되지 않으니 주의할 것.

`npm test` 는 기계를 건드리지 않는다. 설치본 검사는 실제로 설치·제거를 하므로
`npm run test:installer` 로 따로 돌린다. `npm run dist` 를 먼저 해야 한다.

## 설치본

electron-builder + NSIS. 사용자별 설치(`perMachine: false`)라 관리자 권한이 필요 없고,
노트가 실행 파일 옆에 놓인다. `C:\Program Files` 에 넣으면 일반 사용자가 그 옆에 쓸 수
없어 이 구조가 깨진다.

`build/installer.nsh` 가 `customRemoveFiles` 를 바꿔 제거할 때 노트를 지킨다. 주의할
점 하나 — **`IfSilent` 로 사람이 있는지 판단하면 안 된다.** oneClick 제거 프로그램은
자기 확인 창을 띄운 뒤 스스로 silent 모드로 전환하므로 그 시점에는 항상 참이다. 명령줄
의 `/S` 를 직접 봐야 한다.

## 업데이트 알림

시작할 때 한 번, GitHub Releases 를 읽어 **버전만** 비교한다. 새 판이 있으면 알리고,
누르면 받는 곳을 연다. 내려받거나 설치하지는 않는다 — 실행 파일을 받아 실행하는
경로는 그 파일이 어디서 왔는지 증명할 수 있어야 하고, 서명이 없는 동안은 증명할
방법이 없다. 그래서 마지막 한 걸음은 사람 손에 남긴다.

두 가지가 이 설계를 떠받친다.

- **망에서 온 URL 은 셸에 닿지 않는다.** 받는 곳 주소는 `update.js` 안의 상수이고,
  피드에서는 버전 번호만 읽고 나머지는 버린다. 피드가 브라우저의 목적지를 고를 수
  있게 되는 순간 그것이 구멍이다
- **실패한 확인은 침묵한다.** 응답이 없거나, 늦거나, 망가졌거나, 설치된 것보다 옛
  판이면 전부 같은 뜻이다 — 할 말이 없다. 글 쓰는 사람을 방해할 만한 망 사정은 없다

`update.check` 로 끌 수 있다 (설정 창). 패키징되지 않은 앱에서는 확인하지 않는다 —
`package.json` 의 버전이 작업 트리 사정에 따라 아무 값이나 되기 때문이다.

## 데이터 위치

- 배포본 — **exe 옆**의 `BookData` / `csTemplate` / `temp` / `csFreeNote.json`.
  `%APPDATA%` 는 exe 옆이 쓰기 불가일 때만
- 노트 폴더 안 `_backup/` 과 `_images/` 는 트리·검색에서 숨긴다
- 프로젝트 `BookData/` 는 배포되는 기본 노트다. 개인 노트를 여기 두지 말 것
